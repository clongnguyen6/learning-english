# Feature Modules

Planned feature groups:

- `auth`
- `vocabulary`
- `grammar`
- `reading`
- `admin`

This split exists so learner features and admin content operations can evolve without collapsing into a generic page bucket.
